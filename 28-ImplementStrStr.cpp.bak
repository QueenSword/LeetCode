/*=============================================================================
#     FileName: 28-ImplementStrStr.cpp
#         Desc: 
#       Author: qsword
#        Email: huangjian1993@gmail.com
#     HomePage: 
#      Created: 2015-04-06 23:54:13
#      Version: 0.0.1
#   LastChange: 2015-04-06 23:54:13
#      History:
#               0.0.1 | qsword | init
=============================================================================*/

#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    /*int strStr(char *haystack, char *needle) {
        int count = 0;
        while (*haystack != '\0') {
            int index = 0;
            while (*(needle + index) != '\0' && *(haystack + index) == *(needle + index)) {
                index ++;
            }
            if (*(needle + index) == '\0') {
                return count;
            }
            haystack ++;
            count ++;
        }
        return -1;
    }*/
    
    void getNext(const char *needle, vector<int> &next) {
        int k = -1, i = 0;
        next[0] = -1;
        while (i < strlen(needle)) {
            if (k == -1 || p[k] == p[j]) {
                p[++ i] = ++ k;
            } else {
                k = next[k];
            }
        }
        return ;
    }

};

int main() {
    Solution solution;
    char c1[] = "abcdef", c2[] = "bcd";
    cout << solution.strStr(c1, c2) << endl;
    return 0;
}
